export interface Classroom {
    id: string;
    name: string;
    code: string;
    department: string;
    createdBy: string;
    inviteCode: string;
    subjects: string[];
    students: string[];
}